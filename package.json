{
  "name": "ink-pivot",
  "version": "0.1.6",
  "private": true,
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "exceljs": "*"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
